---
import {
  ArrowUpRight,
  BriefcaseBusiness,
  ChartNoAxesCombined,
  Check,
  Database,
  Network,
  Scale,
  ShieldCheck,
} from "lucide-react";
import BlinkText from "@/components/BlinkText";
import type { Locale } from "@/i18n/config";
import type { Messages } from "@/i18n/types";

interface Props {
  locale: Locale;
  messages: Messages;
}

const { locale, messages } = Astro.props;
const { kollektor, intelval, enterprise } = messages.solutions;
const visualCopy =
  locale === "tr"
    ? {
        solution: "Çözüm",
        caseCommand: "Dosya iş akışı",
        queue: "İş kuyruğu",
        case: "Dosya",
        recoveryPath: "Tahsilat akışı",
        score: "Öncelik",
        workflow: "İş akışı",
        review: "İncele",
        contact: "İletişim",
        resolve: "Sonuçlandır",
        analysis: "Analiz",
        method: "Yöntem",
        market: "Piyasa",
        assets: "Varlıklar",
        fabric: "Kurumsal yapay zeka platformu",
        connected: "Bağlı",
        operations: "Operasyon",
        data: "Veri",
        controls: "Kontroller",
        models: "Modeller",
        decisionOutput: "Karar çıktısı",
        context: "Bağlam",
        confidence: "Güven düzeyi",
        control: "Kontrol",
        ready: "Hazır",
        active: "Aktif",
        humanReviewReady: "İnsan denetimi",
      }
    : {
        solution: "Solution",
        caseCommand: "Case workflow",
        queue: "Work queue",
        case: "Case",
        recoveryPath: "Recovery path",
        score: "Priority",
        workflow: "Workflow",
        review: "Review",
        contact: "Contact",
        resolve: "Resolve",
        analysis: "Analysis",
        method: "Method",
        market: "Market",
        assets: "Assets",
        fabric: "Enterprise AI platform",
        connected: "Connected",
        operations: "Operations",
        data: "Data",
        controls: "Controls",
        models: "Models",
        decisionOutput: "Decision output",
        context: "Context",
        confidence: "Confidence level",
        control: "Control",
        ready: "Ready",
        active: "Active",
        humanReviewReady: "Human review",
      };
---

<section
  id="solutions"
  class="mx-auto w-full max-w-[83.75rem] scroll-mt-16 border-x border-b border-border"
  aria-labelledby="solutions-title"
>
  <div
    class="grid grid-cols-1 border-b border-border lg:grid-cols-12"
  >
    <div class="px-5 py-10 sm:px-8 sm:py-14 lg:col-span-7 lg:px-12 lg:py-18">
      <p
        class="mb-5 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.18em] text-brand"
      >
        <span class="h-px w-5 bg-brand/60" aria-hidden="true"></span>
        {messages.intro.eyebrow}
      </p>
      <h2
        id="solutions-title"
        class="max-w-3xl text-balance text-3xl font-medium leading-[1.05] tracking-[-0.035em] sm:text-5xl"
      >
        <BlinkText client:visible text={messages.intro.title} />
      </h2>
    </div>
    <div
      class="flex items-end border-t border-border px-5 py-8 sm:px-8 lg:col-span-5 lg:border-t-0 lg:border-l lg:px-10 lg:py-14"
    >
      <p class="max-w-lg text-pretty text-base leading-relaxed text-muted-foreground">
        {messages.intro.description}
      </p>
    </div>
  </div>

  <div class="grid grid-cols-1 gap-px bg-border md:grid-cols-2 xl:grid-cols-12">
    <article
      class="group relative flex min-h-[41rem] flex-col overflow-hidden bg-background md:col-span-1 xl:col-span-7"
    >
      <div class="relative z-10 px-5 pt-8 sm:px-8 sm:pt-10">
        <div class="mb-8 flex items-start justify-between gap-5">
          <span
            class="flex size-10 items-center justify-center border border-border bg-card text-brand"
          >
            <Scale aria-hidden="true" className="size-4" strokeWidth={1.5} />
          </span>
          <span
            class="font-mono text-[9px] uppercase tracking-[0.18em] text-foreground/30"
          >
            {visualCopy.solution} / 01
          </span>
        </div>

        <p
          class="mb-3 font-mono text-[10px] uppercase tracking-[0.18em] text-brand"
        >
          {kollektor.eyebrow}
        </p>
        <h3 class="text-3xl font-medium tracking-[-0.03em] sm:text-4xl">
          {kollektor.title}
        </h3>
        <p class="mt-4 max-w-2xl text-base leading-relaxed text-muted-foreground">
          {kollektor.description}
        </p>

        <ul class="mt-6 flex flex-wrap gap-x-5 gap-y-2">
          {
            kollektor.highlights.map((highlight) => (
              <li
                class="flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.08em] text-foreground/55"
              >
                <Check
                  aria-hidden="true"
                  className="size-3 text-brand"
                  strokeWidth={2}
                />
                {highlight}
              </li>
            ))
          }
        </ul>
      </div>

      <div
        class="relative mt-10 min-h-64 flex-1 px-5 sm:px-8"
        aria-hidden="true"
      >
        <div
          class="panel-shadow absolute right-0 bottom-0 left-5 border border-b-0 border-foreground/15 bg-card sm:left-8"
        >
          <div
            class="flex h-10 items-center justify-between border-b border-border px-4"
          >
            <div class="flex items-center gap-2">
              <span class="size-1.5 bg-brand"></span>
              <span
                class="font-mono text-[8px] uppercase tracking-[0.16em] text-foreground/55"
              >
                Kollektor / {visualCopy.caseCommand}
              </span>
            </div>
            <span
              class="font-mono text-[8px] uppercase tracking-[0.12em] text-foreground/25"
            >
              {visualCopy.queue}
            </span>
          </div>

          <div class="grid grid-cols-[1fr_5rem] sm:grid-cols-[1fr_7rem]">
            <div class="min-w-0">
              <div
                class="grid grid-cols-[4rem_1fr_3rem] border-b border-border px-3 py-2 font-mono text-[7px] uppercase tracking-[0.14em] text-foreground/25 sm:grid-cols-[5rem_1fr_4rem]"
              >
                <span>{visualCopy.case}</span>
                <span>{visualCopy.recoveryPath}</span>
                <span class="text-right">{visualCopy.score}</span>
              </div>
              {
                [
                  { id: "K-2084", value: "82", width: "82%" },
                  { id: "K-1973", value: "68", width: "68%" },
                  { id: "K-1911", value: "91", width: "91%" },
                  { id: "K-1865", value: "54", width: "54%" },
                ].map((row, index) => (
                  <div
                    class:list={[
                      "grid h-11 grid-cols-[4rem_1fr_3rem] items-center px-3 font-mono text-[8px] sm:grid-cols-[5rem_1fr_4rem]",
                      index !== 3 ? "border-b border-border" : "",
                    ]}
                  >
                    <span class="text-foreground/45">{row.id}</span>
                    <span class="mr-3 h-1 bg-foreground/[0.06]">
                      <span
                        class="block h-full bg-brand/55"
                        style={`width: ${row.width};`}
                      />
                    </span>
                    <span class="text-right text-foreground/30">{row.value}</span>
                  </div>
                ))
              }
            </div>

            <div class="border-l border-border p-3">
              <p
                class="font-mono text-[7px] uppercase tracking-[0.14em] text-foreground/25"
              >
                {visualCopy.workflow}
              </p>
              <div class="mt-4 space-y-3">
                {
                  [
                    visualCopy.review,
                    visualCopy.contact,
                    visualCopy.resolve,
                  ].map((step, index) => (
                    <div class="flex items-center gap-2">
                      <span
                        class:list={[
                          "size-1.5",
                          index < 2
                            ? "bg-brand"
                            : "border border-foreground/20",
                        ]}
                      />
                      <span
                        class:list={[
                          "font-mono text-[7px] uppercase tracking-[0.08em]",
                          index < 2 ? "text-foreground/50" : "text-foreground/20",
                        ]}
                      >
                        {step}
                      </span>
                    </div>
                  ))
                }
              </div>
            </div>
          </div>
        </div>
      </div>

      <a
        href="#demo"
        class="relative z-20 flex min-h-14 items-center justify-between border-t border-border bg-background px-5 text-sm outline-none transition-colors group-hover:bg-foreground/[0.025] focus-visible:bg-foreground/[0.05] focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-inset sm:px-8"
      >
        {kollektor.cta}
        <ArrowUpRight
          aria-hidden="true"
          className="size-4 text-brand transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5"
          strokeWidth={1.5}
        />
      </a>
    </article>

    <article
      class="group relative flex min-h-[41rem] flex-col overflow-hidden bg-background md:col-span-1 xl:col-span-5"
    >
      <div class="relative z-10 px-5 pt-8 sm:px-8 sm:pt-10">
        <div class="mb-8 flex items-start justify-between gap-5">
          <span
            class="flex size-10 items-center justify-center border border-border bg-card text-brand"
          >
            <ChartNoAxesCombined
              aria-hidden="true"
              className="size-4"
              strokeWidth={1.5}
            />
          </span>
          <span
            class="font-mono text-[9px] uppercase tracking-[0.18em] text-foreground/30"
          >
            {visualCopy.solution} / 02
          </span>
        </div>

        <p
          class="mb-3 font-mono text-[10px] uppercase tracking-[0.18em] text-brand"
        >
          {intelval.eyebrow}
        </p>
        <h3 class="text-3xl font-medium tracking-[-0.03em] sm:text-4xl">
          {intelval.title}
        </h3>
        <p class="mt-4 text-base leading-relaxed text-muted-foreground">
          {intelval.description}
        </p>

        <ul class="mt-6 space-y-2">
          {
            intelval.highlights.map((highlight) => (
              <li
                class="flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.08em] text-foreground/55"
              >
                <Check
                  aria-hidden="true"
                  className="size-3 text-brand"
                  strokeWidth={2}
                />
                {highlight}
              </li>
            ))
          }
        </ul>
      </div>

      <div class="relative mt-10 flex flex-1 items-end px-5 sm:px-8" aria-hidden="true">
        <div
          class="panel-shadow w-full border border-b-0 border-foreground/15 bg-card"
        >
          <div
            class="flex h-10 items-center justify-between border-b border-border px-4"
          >
            <span
              class="font-mono text-[8px] uppercase tracking-[0.16em] text-foreground/55"
            >
              Intelval / {visualCopy.analysis}
            </span>
            <span class="size-1.5 bg-brand"></span>
          </div>

          <div class="grid grid-cols-3 border-b border-border">
            {
              [
                { value: "DCF", label: visualCopy.method },
                { value: "MKT", label: visualCopy.market },
                { value: "NAV", label: visualCopy.assets },
              ].map((item, index) => (
                <div
                  class:list={[
                    "px-3 py-4",
                    index !== 2 ? "border-r border-border" : "",
                  ]}
                >
                  <p class="font-mono text-lg text-foreground/80">{item.value}</p>
                  <p
                    class="mt-1 font-mono text-[7px] uppercase tracking-[0.12em] text-foreground/25"
                  >
                    {item.label}
                  </p>
                </div>
              ))
            }
          </div>

          <div
            class="relative flex h-36 items-end gap-2 overflow-hidden px-4 pt-5"
            style="background-image: linear-gradient(to bottom, var(--grid-line-subtle) 1px, transparent 1px); background-size: 100% 25%;"
          >
            {
              [34, 47, 42, 61, 56, 76, 68, 88, 81].map((height, index) => (
                <span
                  class:list={[
                    "min-w-0 flex-1",
                    index === 7 ? "bg-brand" : "bg-foreground/10",
                  ]}
                  style={`height: ${height}%;`}
                />
              ))
            }
          </div>
        </div>
      </div>

      <a
        href="#demo"
        class="relative z-20 flex min-h-14 items-center justify-between border-t border-border bg-background px-5 text-sm outline-none transition-colors group-hover:bg-foreground/[0.025] focus-visible:bg-foreground/[0.05] focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-inset sm:px-8"
      >
        {intelval.cta}
        <ArrowUpRight
          aria-hidden="true"
          className="size-4 text-brand transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5"
          strokeWidth={1.5}
        />
      </a>
    </article>

    <article
      class="group relative grid min-h-[35rem] overflow-hidden bg-background md:col-span-2 xl:col-span-12 xl:grid-cols-12"
    >
      <div
        class="relative z-10 flex flex-col px-5 py-8 sm:px-8 sm:py-10 xl:col-span-5 xl:border-r xl:border-border xl:px-12 xl:py-12"
      >
        <div class="mb-10 flex items-start justify-between gap-5">
          <span
            class="flex size-10 items-center justify-center border border-border bg-card text-brand"
          >
            <Network aria-hidden="true" className="size-4" strokeWidth={1.5} />
          </span>
          <span
            class="font-mono text-[9px] uppercase tracking-[0.18em] text-foreground/30"
          >
            {visualCopy.solution} / 03
          </span>
        </div>

        <p
          class="mb-3 font-mono text-[10px] uppercase tracking-[0.18em] text-brand"
        >
          {enterprise.eyebrow}
        </p>
        <h3 class="max-w-xl text-3xl font-medium tracking-[-0.03em] sm:text-4xl">
          {enterprise.title}
        </h3>
        <p
          class="mt-4 max-w-xl text-base leading-relaxed text-muted-foreground"
        >
          {enterprise.description}
        </p>

        <ul class="mt-7 space-y-2">
          {
            enterprise.highlights.map((highlight) => (
              <li
                class="flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.08em] text-foreground/55"
              >
                <Check
                  aria-hidden="true"
                  className="size-3 text-brand"
                  strokeWidth={2}
                />
                {highlight}
              </li>
            ))
          }
        </ul>

        <a
          href="#platform"
          class="mt-9 inline-flex w-fit items-center gap-2 border-b border-foreground/20 pb-1 text-sm outline-none transition-colors hover:border-brand hover:text-brand focus-visible:ring-2 focus-visible:ring-brand"
        >
          {enterprise.cta}
          <ArrowUpRight
            aria-hidden="true"
            className="size-4 transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5"
            strokeWidth={1.5}
          />
        </a>
      </div>

      <div
        class="relative flex min-h-[27rem] items-center justify-center overflow-hidden border-t border-border bg-card/35 p-5 sm:p-8 xl:col-span-7 xl:min-h-0 xl:border-t-0 xl:p-12"
        aria-hidden="true"
      >
        <div
          class="pointer-events-none absolute inset-0"
          style="background-image: radial-gradient(var(--dot-grid-color) 0.7px, transparent 0.7px); background-size: 14px 14px;"
        >
        </div>

        <div class="relative w-full max-w-2xl border border-foreground/15 bg-background">
          <div
            class="flex h-10 items-center justify-between border-b border-border px-4"
          >
            <span
              class="font-mono text-[8px] uppercase tracking-[0.16em] text-foreground/50"
            >
              {visualCopy.fabric}
            </span>
            <span
              class="font-mono text-[8px] uppercase tracking-[0.12em] text-brand"
            >
              {visualCopy.connected}
            </span>
          </div>

          <div class="grid grid-cols-1 sm:grid-cols-[1fr_4rem_1fr]">
            <div class="grid grid-cols-2 gap-px bg-border">
              {
                [
                  { label: visualCopy.operations, Icon: BriefcaseBusiness },
                  { label: visualCopy.data, Icon: Database },
                  { label: visualCopy.controls, Icon: ShieldCheck },
                  { label: visualCopy.models, Icon: ChartNoAxesCombined },
                ].map(({ label, Icon }) => (
                  <div
                    class="flex min-h-24 flex-col justify-between bg-card/60 p-3"
                  >
                    <Icon
                      className="size-4 text-foreground/35"
                      strokeWidth={1.25}
                    />
                    <span
                      class="font-mono text-[8px] uppercase tracking-[0.12em] text-foreground/40"
                    >
                      {label}
                    </span>
                  </div>
                ))
              }
            </div>

            <div
              class="relative hidden border-x border-border sm:flex sm:items-center sm:justify-center"
            >
              <span class="absolute inset-y-0 left-1/2 w-px bg-border"></span>
              <span
                class="relative z-10 flex size-7 items-center justify-center border border-brand/40 bg-background"
              >
                <span class="size-1.5 bg-brand"></span>
              </span>
            </div>

            <div class="border-t border-border p-4 sm:border-t-0">
              <p
                class="font-mono text-[8px] uppercase tracking-[0.14em] text-foreground/30"
              >
                {visualCopy.decisionOutput}
              </p>
              <div class="mt-5 space-y-4">
                {
                  [
                    {
                      label: visualCopy.context,
                      width: "88%",
                      status: visualCopy.ready,
                    },
                    {
                      label: visualCopy.confidence,
                      width: "72%",
                      status: visualCopy.active,
                    },
                    {
                      label: visualCopy.control,
                      width: "94%",
                      status: visualCopy.ready,
                    },
                  ].map((item) => (
                    <div>
                      <div
                        class="mb-2 flex justify-between font-mono text-[8px] uppercase tracking-[0.1em] text-foreground/35"
                      >
                        <span>{item.label}</span>
                        <span>{item.status}</span>
                      </div>
                      <div class="h-1 bg-foreground/[0.06]">
                        <div
                          class="h-full bg-brand/55"
                          style={`width: ${item.width};`}
                        />
                      </div>
                    </div>
                  ))
                }
              </div>
              <div
                class="mt-6 flex items-center gap-2 border border-brand/20 bg-brand/[0.06] px-3 py-2"
              >
                <span class="size-1.5 bg-brand"></span>
                <span
                  class="font-mono text-[7px] uppercase tracking-[0.12em] text-brand"
                >
                  {visualCopy.humanReviewReady}
                </span>
              </div>
            </div>
          </div>

          <span
            class="absolute -left-px -top-px size-2.5 border-l border-t border-brand/70"
          ></span>
          <span
            class="absolute -right-px -bottom-px size-2.5 border-r border-b border-brand/70"
          ></span>
        </div>
      </div>

      <span
        class="absolute right-0 bottom-0 size-3 border-r border-b border-foreground/25"
        aria-hidden="true"
      ></span>
    </article>
  </div>
</section>
